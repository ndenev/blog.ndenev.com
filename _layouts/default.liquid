<!DOCTYPE html>
<html>
    <head>
        {% if is_post %}
          <title>{{ title }}</title>
        {% else %}
       	  <title>blog.ndenev.com</title>
        {% endif %}
    </head>
    <body>
    <div>
      {% if is_post %}
        {% include '_layouts/post.liquid' %}
      {% else %}
        {{ content }}
      {% endif %}
    </div>
  </body>
</html>
